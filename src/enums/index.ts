export enum FriendshipStatus {
  PENDING = "PENDING",
  ACCEPTED = "ACCEPTED",
  REJECTED = "REJECTED",
}

export enum ReelVisibility {
  PUBLIC = "PUBLIC",
  FRIENDS = "FRIENDS",
  CUSTOM = "CUSTOM",
  PRIVATE = "PRIVATE",
}
